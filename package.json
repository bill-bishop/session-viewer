{
  "name": "session-viewer",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "session-viewer": "./dist/src/server.js"
  },
  "scripts": {
    "build": "tsc && cp -r src/public dist/src/public",
    "start": "ts-node src/server.ts",
    "test": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0",
    "vitest": "^4.1.2"
  }
}
